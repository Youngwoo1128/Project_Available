<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".BoastEditActivity"
    android:background="#91AAB8">

    <ImageView
        android:id="@+id/iv"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@drawable/bg_boast_edit"
        android:src="@drawable/ic_baseline_image_24"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="사진 올리기"
        android:layout_gravity="right"
        android:backgroundTint="#91634D"
        android:onClick="clickBoast"/>

    <EditText
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="닉네임을 입력해 주세요"
        android:padding="8dp"
        android:inputType="text"
        android:background="@drawable/bg_boast_edit"/>

    <EditText
        android:id="@+id/msg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="자랑할 글을 적어주세요!!"
        android:inputType="textMultiLine"
        android:padding="8dp"
        android:background="@drawable/bg_boast_edit"
        android:layout_weight="1"
        android:layout_marginTop="12dp"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="올리기"
        android:backgroundTint="#91634D"
        android:layout_gravity="right"
        android:onClick="clickBoastUpload"/>


</LinearLayout>